# CLI application

The `eventlens` console command is packaged from `eventlens.cli` to avoid an extra deployment dependency for the first monorepo release. Run `eventlens demo` from the repository root after installing the root Python project. It consumes local files only.
